# Copy this file to .env and set your own password.
# The .env file is git-ignored and must never be committed.
#
# SQL Server password policy: at least 8 characters,
# with uppercase, lowercase, digits and symbols.
MSSQL_SA_PASSWORD=ChangeMe_123!
